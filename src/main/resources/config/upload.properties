# 첨부파일 저장 설정 (팀 공통 템플릿)
# 각자 PC 경로가 다르면 같은 폴더에 upload.local.properties 로 upload.root.path 만 덮어쓰기

# 실제 파일 저장 루트 (절대경로, Tomcat 돌리는 PC 기준)
upload.root.path=D:/upload/bit_gongbang

# 파일 1개 최대 5MB
upload.max.size=5242880

# 요청 전체 최대 60MB (다중 업로드)
upload.max.request.size=62914560
